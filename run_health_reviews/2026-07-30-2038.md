VERDICT: PROBLEM

# Post-Run Health Review — 2026-07-30 (post-EOD fire, ~16:38 ET)

## Context
- ET time at review start: 2026-07-30 16:38 EDT (Thursday).
- `preflight_gate.py` → `gate_exit=20` (SKIP/DONE — already executed today, mode=`risk_watch`, run_id `20260730-134855`). Confirms today's capital-path decision (risk_watch, 0 stops) has not been re-run or altered since the post-morning review.
- This is the **post-EOD fire**: the EOD routine ran (no trading pipeline, no orders — writes `mcp_portfolio.json`/`portfolio_snapshot.json` only), landing commit `0fffab9 chore: eod portfolio snapshot` at 2026-07-30T20:09:00Z, immediately after the post-morning review's commit (`4ec293b`).
- This review is a **continuation check** on the CODE bug already reported by the post-morning review (`4ec293b`), not a rediscovery — see below.

## What ran vs. expectations

### EOD close snapshot — HEALTHY
- Commit `0fffab9` touched exactly the two expected files: `mcp_portfolio.json`, `portfolio_snapshot.json`. No `[skip ci]` in the commit message (required so `publish.yml` fires on this push — the documented Jun 12 bug class).
- `portfolio_snapshot.json`: `is_close: true`, `portfolio.as_of = 2026-07-30T16:00:00-04:00` (today, 4 PM ET — correct close timestamp), `total_value = 508.69`, 7 positions consistent with the morning's risk_watch book. `close_value`/`close_at` are (correctly) absent from the local file — those fields are written only into the Supabase row by `publish.py`'s `--close` path (lines ~319–339), not persisted back to the committed JSON; this matches source and is not a defect.
- `mcp_portfolio.json.as_of` matches the same timestamp — freshness guard satisfied.
- No capital action was possible or taken here by design (EOD places no orders) — nothing to flag.

### Capital path (risk_watch, unchanged since post-morning) — HEALTHY
- `pending_decisions.json`: `date=2026-07-30`, `mode=risk_watch`, `decisions=[]`, `execution_started_at=null`, `executed_at` stamped (13:49:35Z) — correct zero-decision envelope, unchanged and un-tampered-with since this morning.
- `last_rebalance.json`: `iso_week=2026-W31`, `executed_at` set for 2026-07-29 (last Wednesday) — correct once-per-week lock, not re-claimed today (as expected for a risk_watch day).
- `system_health.json` is still the 13:48:55Z (risk_watch) record — expected, since the EOD routine does not re-run health checks.
- `trades.csv` / `transactions.json`: no new rows since 2026-07-22 — consistent with 0 stops fired.
- `git status` clean before this review's own writes.

### Research/data-plane producer (`market_data.yml`) — STILL BROKEN (same CODE bug as post-morning, unresolved)
- `heartbeat_check.py` (read-only): `ALERT (data_plane_fresh=False)` — `market_snapshot`, `data_quality_report`, `factor_history`, `research_dossier` all still dated **2026-07-29** (one day stale).
- Checked GitHub Actions `market_data.yml` runs since the post-morning review (`4ec293b`, ~13:38 ET): **no new runs have fired**. The last 4 attempts today (07:04, 08:23, 10:21, 10:42 ET) are the same 4 failures already diagnosed this morning as an unhandled `ZeroDivisionError` in `market_data.py:get_price()` (the `len(history) >= 2` branch divides by `prev["close"]` with no zero-guard, unlike the sibling `len==1` branch). No fifth attempt since, and no fix has landed on `main` (`git log 4ec293b..HEAD` shows only the EOD commit).
- No open `health-alert` or `heartbeat-alert` GitHub issue exists (checked directly).
- **Impact, unchanged from this morning's assessment:** not capital-sensitive today (risk_watch ran correctly on live MCP prices, needs no snapshot by design). Still on track to **block next Wednesday's rebalance (2026-08-05)** if the bug isn't fixed before then — `main.py`/`preflight_gate.py` require a same-day snapshot with ≥22 bars and a fresh, valid dossier; a persistent break means the gate will correctly SKIP-RETRY rather than trade on stale data (fails safe), but risks the week's rebalance being missed entirely.
- **No action re-attempted this review** — the post-morning review already attempted the STEP 5a auto-fix (`gh workflow run market_data.yml`) and it failed with `403 Resource not accessible by integration` (this reviewer's GitHub App token cannot dispatch workflows). Re-attempting would fail identically; not repeated to avoid noise. The fix remains: a human-reviewed session must patch `market_data.py:get_price()`'s missing zero-guard (see the post-morning report, `run_health_reviews/2026-07-30-*post-morning*.md`, for the full diagnosis and minimal repro) before 2026-08-05.

## Findings summary

| Severity | Category | Evidence | Action |
|---|---|---|---|
| Healthy | operational | EOD commit `0fffab9`: correct files, no `[skip ci]`, `is_close=true`, fresh `as_of` | none needed |
| Healthy | operational | risk_watch envelope/health/journal all unchanged and consistent with the post-morning review | none needed |
| **Problem (persisting, not new)** | **code** | `market_data.yml` still failing (last attempt 10:42 ET, same `ZeroDivisionError` in `market_data.py:get_price()`); data plane still dated 2026-07-29; no fix commit since `4ec293b` | Needs a reviewed source fix before the 2026-08-05 rebalance Wednesday — not applied by this reviewer (governance rule). Auto-fix dispatch already attempted and blocked (403) this morning; not repeated. |

This is a continuation of an already-reported issue, not a new discovery — flagged `PROBLEM` again because the underlying condition (stale data plane, broken producer) remains true and unresolved at this review's timestamp, and the rebalance-blocking risk grows closer with each day it's unfixed.

No files outside `RUN_REVIEW.md` / `run_health_reviews/` were modified by this review.
